<% flash.each do |type, msg| %>
  <p><%= msg %></p>
<% end %>
<%= form_with url: "/login", method: :post, local: true do |f| %>
  <%= f.label "email" %>
  <%= f.email_field "email" %>
  <%= f.label "password" %>
  <%= f.password_field "password" %>

  <%= f.submit "Log In" %>
<% end %>
